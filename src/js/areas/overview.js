
// glyphr.overview

{
	init() {
		
	},
	dispatch(event) {
		let APP = glyphr,
			Self = APP.overview,
			el;
		// console.log(event);
		switch (event.type) {
			case "init-mode":
				break;
		}
	}
}
