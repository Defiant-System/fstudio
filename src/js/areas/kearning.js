
// glyphr.kearning

{
	init() {
		
	},
	dispatch(event) {
		let APP = glyphr,
			Self = APP.kearning,
			el;
		// console.log(event);
		switch (event.type) {
			case "init-mode":
				break;
		}
	}
}
